// Package ir contains the intermediate representation (IR) for the Angular
// template compilation pipeline. This IR is constructed during ingestion and
// transformed by a series of pipeline phases before being emitted.
package ir
